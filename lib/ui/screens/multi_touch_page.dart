import 'package:flutter/material.dart';

class MultiTouchPage extends StatelessWidget {
  const MultiTouchPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
